version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  apt_packages:
    - graphviz
    - default-jre
    - make

sphinx:
  configuration: docs/source/conf.py

formats:
  - htmlzip

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - doc

